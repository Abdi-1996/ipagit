name: VideoToLivePhoto
options:
  bundleIdPrefix: com.abdi
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    SWIFT_VERSION: 5.0
    IPHONEOS_DEPLOYMENT_TARGET: 17.0
targets:
  VideoToLivePhoto:
    type: application
    platform: iOS
    sources:
      - path: VideoToLivePhoto
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.abdi.videotolivephoto
        PRODUCT_NAME: VideoToLivePhoto
        INFOPLIST_FILE: VideoToLivePhoto/Info.plist
        TARGETED_DEVICE_FAMILY: "1"
        SUPPORTS_MACCATALYST: NO
        CODE_SIGN_STYLE: Automatic
    scheme:
      testTargets: []
